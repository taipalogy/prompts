> @taipalogy
---

# Inter-Instance Accretion Protocol: The Master Pattern Library

This library codifies the **IIA Protocol**, where sovereign **Modules** are deconstructed and recomposed via **Synthesized Interfaces**. It functions as a standalone architectural authority, independent of the **Sentence Editing Protocol**.

**Core Integration**: IIA is execution-agnostic. It governs **Structural Accretion** (logic/intent), while technologies like **OpenClaw** provide **Physical Grounding** (commits/sync). IIA pours; the gateway grounds.

---

### 1. Unified Core Pattern Registry (Standalone)

* **The Vellum Translation**
    `anchor:Material | utilize:filter(Debt) | utilize:map(Syntax)`
    **Refinery**: Systematic entropy subtraction to purify raw material via **Cursor** or **Codex**.

* **The Contextual Swarm**
    `anchor:Brief | utilize:map(Reqs) | utilize:reduce(Blueprint)`
    **Type Synthesis**: Deriving strict logical types from documentation via **Claude Code**.

* **The Autonomous Loop**
    `anchor:State | (utilize:curry(Valency) | utilize:map(Optim))`
    **Autonomous Audit**: Recursive self-healing and state-validation via **Playwright**.

* **The Logic Injector**
    `anchor:Schema | utilize:map(Actions) | utilize:reduce(Wiring)`
    **UI-to-Logic Sync**: Hardening Backend-Frontend bonds from **Figma** design tokens.

* **The Supervisor Dispatch**
    `anchor:Global | utilize:map(Supervisor) | utilize:curry(Specialist)`
    **Logic Decoupling**: Intent-execution separation orchestrated through **Claude Code CLI**.

---

### 2. Tool-Specific & Compensatory Patterns

* **The Kinetic Multi-Tenant (Workspace + Security Hardening)**
    `anchor:Tenant_Config | (utilize:curry(Workspace_CLI) | utilize:map(Role_Synthesis)) | utilize:reduce(Federated_Chassis)`
    **Logic**: Probes state via **Claude Code CLI**, currying strict ACLs into the synthesized interface.

* **The Ghost-Bypass (Logic Decoupling)**
    `anchor:Legacy_Silo | utilize:filter(Opaque_Logic) | utilize:curry(Deconstruction) | utilize:reduce(Translucent_Interface)`
    **Logic**: Synthesizes a proxy for **Cursor** to decouple "Black Box" behavior from the gateway.

* **The Alabaster Refiner (Figma-to-Code Sync)**
    `anchor:Figma_Export | utilize:filter(Noise) | utilize:map(Swiss_Baseline) | utilize:reduce(0.5px_Hairline)`
    **Logic**: Visual token mapping that enforces the **0/64 Law** during terminal grounding.

---

### 3. Functional Primitives: The Metabolic Drivers

* **Map (Transformation)**: Ensures **Interface Parity** during cross-tool translation.
* **Filter (Refinement)**: Primary mechanism for **Entropy Subtraction** and noise removal.
* **Reduce (Consolidation)**: Collapses data streams into a hardened, high-density **Sovereign Blueprint**.
* **Curry (Configuration)**: Pre-loads modules with **Contextual Injection** (e.g., ACLs) before deployment.

---

### 4. The Mechanism of the Structural Ripple

**Dependency ripples are dynamically generated on demand by referencing the Sentence Editing Protocol's logic to calculate inter-instance displacement and state synchronization.**

---

### 5. Operational Laws of the IIA Protocol (Reserved)

1.  **The Law of Synthesized Interfaces**: Interfaces are "poured" at runtime based on **Type Synthesis** and deconstruction.
2.  **The Law of Material Density**: Every `reduce` must increase type-safety and harden security.
3.  **The Law of Primitive Selection**: All primitives are ground-level execution. Use **Functional** for data-shaping and **Shell** for file-shaping.
4.  **The Law of Gateway Agnosticism**: Intent is independent of the commit tool (**OpenClaw** or similar).
5.  **The Ripple Exclusion**: When **Sentence Editing** is absent, ripples affect **Module States** and **Geometry**, not text strings.

---

